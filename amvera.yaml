meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: env | grep -e NEXT_PUBLIC_ > .env.production ; npm ci ; npm run build

run:
  command: npm run start
  containerPort: 3000

deploy:
  ignore:
    - node_modules/
    - .next/
    - .git/
