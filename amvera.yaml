meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  command: npm ci && npm run build

run:
  command: npm run start
  containerPort: 3000

deploy:
  ignore:
    - node_modules/
    - .next/
    - .git/
